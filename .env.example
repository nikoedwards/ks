# Web remains stateless and must never run crawler or sync jobs.
KICKSONAR_JOBS_ENABLED=0

# Disabled by default. Enable only on a staging Web deployment first.
KICKSONAR_CORE_PROXY_ENABLED=0
KICKSONAR_CORE_PROXY_GROUPS=public
KICKSONAR_CORE_BASE_URL=http://kicksonar-core-staging.railway.internal
